{
  "name": "kiddyphone-swml-poc",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "KiddyPhone whitelist gating on SWML: answer_on_bridge holds the caller unanswered. Three flows: single webhook, park + REST update, in-script request. Instrumented to settle U4 (webhook response budget) and U5 (webhook request body shape).",
  "engines": {
    "node": ">=20.12"
  },
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test 'test/*.test.mjs'"
  },
  "dependencies": {
    "@signalwire/realtime-api": "^4.2.1",
    "libphonenumber-js": "^1.11.0"
  }
}
